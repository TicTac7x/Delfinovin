<Window
    x:Class="Delfinovin.Controls.Windows.ThemeSelectorMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:Delfinovin.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:main="clr-namespace:Delfinovin"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Static main:Strings.HeaderThemeSelector}"
    Width="400"
    Height="215"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    WindowStyle="None"
    mc:Ignorable="d">

    <Window.Resources>
        <ResourceDictionary>
            <Style BasedOn="{StaticResource MaterialDesignFlatAccentBgButton}" TargetType="{x:Type Button}">
                <Setter Property="Height" Value="30" />
                <Setter Property="Margin" Value="0,5,7,10" />
                <Setter Property="BorderBrush" Value="Black" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="Background" Value="{DynamicResource normalButtonColor}" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <Border
            x:Name="mainBackground"
            Grid.RowSpan="3"
            Background="{DynamicResource headerGradient}"
            BorderBrush="Black"
            BorderThickness="2"
            CornerRadius="15,15,15,15" />

        <Border
            x:Name="navigationBarBackground"
            Grid.Row="2"
            Background="{DynamicResource controllerBarGradient}"
            BorderBrush="Black"
            BorderThickness="2"
            CornerRadius="0,0,15,15" />

        <ctrl:ApplicationTitleLabel x:Name="themeWindowTitle" HeaderText="{x:Static main:Strings.HeaderThemeSelector}" />

        <ctrl:WindowControls
            x:Name="windowControls"
            HorizontalAlignment="Right"
            MinimizeButtonVisibility="Collapsed" />

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Left"
            VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="{x:Type ctrl:ComboBoxListItem}">
                    <Setter Property="HorizontalAlignment" Value="Left" />
                    <Setter Property="Margin" Value="10,0,0,5" />
                </Style>
            </StackPanel.Resources>

            <ctrl:ComboBoxListItem x:Name="applicationTheme" ItemText="{x:Static main:Strings.ApplicationTheme}" />

            <ctrl:ComboBoxListItem x:Name="controllerColor" ItemText="{x:Static main:Strings.ControllerColor}" />
        </StackPanel>


        <Button
            x:Name="saveTheme"
            Grid.Row="3"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Click="SaveTheme_Click"
            Content="{x:Static main:Strings.SaveTheme}" />

    </Grid>
</Window>
