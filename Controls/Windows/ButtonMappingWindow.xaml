<Window
    x:Class="Delfinovin.Controls.Windows.ButtonMappingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:Delfinovin.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:main="clr-namespace:Delfinovin"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Static main:Strings.HeaderButtonMapping}"
    Width="550"
    Height="550"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    WindowStyle="None"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="215" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style BasedOn="{StaticResource MaterialDesignFlatAccentBgButton}" TargetType="{x:Type Button}">
                <Setter Property="Height" Value="30" />
                <Setter Property="Margin" Value="0,5,7,10" />
                <Setter Property="BorderBrush" Value="Black" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="Background" Value="{DynamicResource normalButtonColor}" />
            </Style>
        </Grid.Resources>

        <Border
            x:Name="mainBackground"
            Grid.RowSpan="3"
            Grid.ColumnSpan="2"
            Background="{DynamicResource headerGradient}"
            BorderBrush="Black"
            BorderThickness="2"
            CornerRadius="15,15,15,15" />

        <Border
            x:Name="optionsBar"
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Background="{DynamicResource controllerBarGradient}"
            BorderBrush="Black"
            BorderThickness="2"
            CornerRadius="0,0,15,15" />

        <ctrl:ApplicationTitleLabel x:Name="mappingWindowTitle" HeaderText="{x:Static main:Strings.HeaderButtonMapping}" />

        <ctrl:WindowControls
            x:Name="windowControls"
            Grid.Column="1"
            HorizontalAlignment="Right" />

        <ListView
            x:Name="outputButtonListView"
            Grid.Row="1"
            Margin="5,5,5,5"
            HorizontalAlignment="Left"
            Background="Transparent"
            BorderBrush="Transparent">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Height" Value="60" />
                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OutputButtonListViewItem_PreviewMouseLeftButtonDown" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <Border
                                    x:Name="Bd"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5"
                                    SnapsToDevicePixels="False">

                                    <ContentPresenter
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{TemplateBinding Content}"
                                        ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Border>

                                <ControlTemplate.Triggers>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsMouseOver" Value="True" />
                                        </MultiTrigger.Conditions>

                                        <MultiTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation
                                                        Storyboard.TargetName="Bd"
                                                        Storyboard.TargetProperty="Background.Color"
                                                        To="#2698DBE0"
                                                        Duration="0:0:0.5" />

                                                </Storyboard>
                                            </BeginStoryboard>
                                        </MultiTrigger.EnterActions>

                                        <MultiTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation
                                                        Storyboard.TargetName="Bd"
                                                        Storyboard.TargetProperty="Background.Color"
                                                        To="Transparent"
                                                        Duration="00:00:0.5" />

                                                </Storyboard>
                                            </BeginStoryboard>
                                        </MultiTrigger.ExitActions>
                                    </MultiTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <StackPanel
            x:Name="optionsContainer"
            Grid.Row="2"
            Grid.Column="2"
            Margin="5,5,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">

            <Button
                x:Name="saveSettings"
                Click="SaveSettings_Click"
                Content="{x:Static main:Strings.SaveSettings}" />

        </StackPanel>

        <ctrl:ImageListItem
            x:Name="selectedMappingDisplay"
            Grid.Row="1"
            Grid.Column="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Top" />

        <ListView
            x:Name="mappingButtonListView"
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,15,0,0"
            SelectionChanged="MappingButtonListView_SelectionChanged">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="5" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <StackPanel
            x:Name="mappingButtonControls"
            Grid.Row="1"
            Grid.Column="1"
            Margin="5,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom"
            Orientation="Horizontal">

            <Button
                x:Name="addMapping"
                Click="AddMapping_Click"
                IsEnabled="False"
                ToolTip="{x:Static main:Strings.ToolTipAddMapping}">

                <Image Source="/Delfinovin;component/Resources/Icons/add.png" />
            </Button>

            <Button
                x:Name="removeMapping"
                Click="RemoveMapping_Click"
                IsEnabled="False"
                ToolTip="{x:Static main:Strings.ToolTipRemoveMapping}">

                <Image Source="/Delfinovin;component/Resources/Icons/minus.png" />
            </Button>
        </StackPanel>
    </Grid>
</Window>
